<%- model_class = ResponseMessage -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:message) %>:</strong></dt>
  <dd><%= @response_message.message %></dd>
  <dt><strong><%= model_class.human_attribute_name(:message_type) %>:</strong></dt>
  <dd><%= @response_message.message_type %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              response_messages_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_response_message_path(@response_message), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              response_message_path(@response_message),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
